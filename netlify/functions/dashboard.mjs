import {db,response} from './_db.mjs';
export async function handler(event){if(event.httpMethod==='OPTIONS')return response(204,{});try{const sql=db();const [k]=await sql`SELECT count(*)::int total_employees,count(*) FILTER(WHERE status='ACTIF')::int active_employees,coalesce(sum(base_salary),0)::numeric payroll_base FROM employees`;const byDepartment=await sql`SELECT d.code,d.name,count(e.id)::int employee_count FROM departments d LEFT JOIN employees e ON e.department_code=d.code GROUP BY d.code,d.name,d.sort_order ORDER BY d.sort_order`;return response(200,{...k,byDepartment})}catch(e){return response(500,{error:e.message})}}
